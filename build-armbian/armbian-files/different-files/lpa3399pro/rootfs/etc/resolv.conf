# Fallback DNS when systemd-resolved is masked or DHCP DNS is unusable.
# NetworkManager may still override when online; start_service also sets NM DNS.
nameserver 8.8.8.8
nameserver 1.1.1.1
nameserver 223.5.5.5
options single-request-reopen
